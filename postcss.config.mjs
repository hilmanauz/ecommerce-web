/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    'postcss-import': {},
    'tailwindcss/nesting': {},
    autoprefixer: {},
  },
}

export default config
